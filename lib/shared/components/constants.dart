import 'package:flutter/material.dart';


const Color MAIN_COLOR = Colors.lightBlue;
final lastDate = DateTime.now().add(const Duration(days: 365 * 2));
final firstDate = DateTime.now().subtract(const Duration(days: 365 * 2));
